(function(){document.body.classList.add('is-loaded');document.querySelectorAll('.ai-logo img').forEach(function(img){if(img.complete&&img.naturalWidth===0){img.style.display='none';}img.addEventListener('error',function(){img.style.display='none';});});var navToggle=document.querySelector('.nav-toggle');var nav=document.querySelector('.main-nav');if(navToggle&&nav){navToggle.addEventListener('click',function(){var open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});}document.addEventListener('click',function(e){var a=e.target.closest('a');if(!a)return;if(a.target||a.hasAttribute('download'))return;var href=a.getAttribute('href')||'';if(!href||href.startsWith('#')||href.startsWith('mailto:')||href.startsWith('http'))return;var url=new URL(href,location.href);if(url.pathname===location.pathname&&url.hash)return;e.preventDefault();document.body.classList.add('is-leaving');setTimeout(function(){location.href=url.href;},320);});var reveals=[].slice.call(document.querySelectorAll('.reveal'));if('IntersectionObserver'in window){var io=new IntersectionObserver(function(entries){entries.forEach(function(en){if(en.isIntersecting){en.target.classList.add('visible');io.unobserve(en.target);}});},{threshold:.12});reveals.forEach(function(el){io.observe(el);});}else{reveals.forEach(function(el){el.classList.add('visible');});}var search=document.getElementById('searchInput');var plan=document.getElementById('planFilter');var cards=[].slice.call(document.querySelectorAll('.tool-card'));var counter=document.getElementById('catalogCounter');var active='all';function clean(v){return String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');}function filter(){var q=clean(search&&search.value);var p=plan?plan.value:'all';var visible=0;cards.forEach(function(card){var text=clean(card.dataset.name+' '+card.textContent);var cats=clean(card.dataset.categories);var plans=clean(card.dataset.plans);var show=(!q||text.indexOf(q)>-1)&&(active==='all'||cats.indexOf(active)>-1)&&(p==='all'||plans.indexOf(p)>-1);card.classList.toggle('is-hidden',!show);if(show)visible++;});if(counter)counter.textContent='Mostrando '+visible+' herramienta'+(visible===1?'':'s')+'.';}document.querySelectorAll('.chip').forEach(function(chip){chip.addEventListener('click',function(){document.querySelectorAll('.chip').forEach(function(c){c.classList.remove('active');});chip.classList.add('active');active=chip.dataset.filter||'all';filter();});});if(search)search.addEventListener('input',filter);if(plan)plan.addEventListener('change',filter);filter();document.querySelectorAll('[data-copy]').forEach(function(btn){btn.addEventListener('click',function(){var text=btn.getAttribute('data-copy')||'';function done(){var old=btn.textContent;btn.textContent='Copiado';setTimeout(function(){btn.textContent=old;},1300);}if(navigator.clipboard&&window.isSecureContext){navigator.clipboard.writeText(text).then(done).catch(function(){btn.textContent='Copia manual';});}else{var area=document.createElement('textarea');area.value=text;area.style.position='fixed';area.style.opacity='0';document.body.appendChild(area);area.focus();area.select();try{document.execCommand('copy');done();}catch(e){btn.textContent='Copia manual';}document.body.removeChild(area);}});});var tilt=document.querySelector('[data-tilt]');if(tilt&&window.matchMedia('(pointer:fine)').matches){tilt.addEventListener('mousemove',function(ev){var r=tilt.getBoundingClientRect();var x=(ev.clientX-r.left)/r.width-.5;var y=(ev.clientY-r.top)/r.height-.5;tilt.style.transform='rotateY('+(x*4).toFixed(2)+'deg) rotateX('+(-y*3).toFixed(2)+'deg)';});tilt.addEventListener('mouseleave',function(){tilt.style.transform='';});}var canvas=document.getElementById('particleCanvas');if(canvas){var ctx=canvas.getContext('2d');var w=0,h=0,parts=[];var reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;function resize(){var r=canvas.getBoundingClientRect();w=Math.max(1,Math.floor(r.width));h=Math.max(1,Math.floor(r.height));var ratio=Math.min(window.devicePixelRatio||1,1.7);canvas.width=Math.floor(w*ratio);canvas.height=Math.floor(h*ratio);ctx.setTransform(ratio,0,0,ratio,0,0);var count=w<720?34:62;parts=Array.from({length:count},function(){return{x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.28,vy:(Math.random()-.5)*.22,r:Math.random()*1.7+.7};});}function draw(){ctx.clearRect(0,0,w,h);ctx.fillStyle='rgba(66,243,255,.62)';ctx.strokeStyle='rgba(89,168,255,.14)';ctx.lineWidth=1;for(var i=0;i<parts.length;i++){var p=parts[i];if(!reduce){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;}ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill();for(var j=i+1;j<parts.length;j++){var q=parts[j];var dx=p.x-q.x;var dy=p.y-q.y;var d=Math.sqrt(dx*dx+dy*dy);if(d<128){ctx.globalAlpha=(128-d)/128;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke();ctx.globalAlpha=1;}}}if(!reduce)requestAnimationFrame(draw);}resize();draw();window.addEventListener('resize',resize);}})();